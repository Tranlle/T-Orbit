<Window x:Class="Tranbok.Tools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Tranbok.Tools.ViewModels"
        mc:Ignorable="d"
        Title="Tranbok Tools"
        Height="780" Width="1260"
        MinHeight="600" MinWidth="900"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{StaticResource BgCanvasBrush}"
        d:DataContext="{d:DesignInstance vm:MainViewModel}">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40"
                      ResizeBorderThickness="6"
                      GlassFrameThickness="-1"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Border Background="{StaticResource BgCanvasBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="{StaticResource BgDefaultBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal"
                            VerticalAlignment="Center" Margin="14,0,0,0">
                    <TextBlock Text="&#xE8F1;"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="16"
                               Foreground="{StaticResource AccentBlueBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>
                    <TextBlock Text="Tranbok Tools"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource FgDefaultBrush}"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="dev"
                               FontSize="10"
                               Foreground="{StaticResource FgSubtleBrush}"
                               VerticalAlignment="Bottom"
                               Margin="6,0,0,3"/>
                </StackPanel>

                <Border Grid.Column="1"
                        Background="Transparent"
                        WindowChrome.IsHitTestVisibleInChrome="False"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal"
                            VerticalAlignment="Stretch"
                            WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button x:Name="MinimizeButton"
                            Click="MinimizeButton_Click"
                            Style="{StaticResource GhostButton}"
                            Width="46" Height="40"
                            VerticalAlignment="Stretch"
                            ToolTip="最小化">
                        <TextBlock Text="&#xE921;"
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="10"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"/>
                    </Button>
                    <Button x:Name="MaximizeButton"
                            Click="MaximizeButton_Click"
                            Style="{StaticResource GhostButton}"
                            Width="46" Height="40"
                            VerticalAlignment="Stretch"
                            ToolTip="最大化">
                        <TextBlock Text="&#xE922;"
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="10"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"/>
                    </Button>
                    <Button x:Name="CloseButton"
                            Click="CloseButton_Click"
                            Style="{StaticResource GhostButton}"
                            Width="46" Height="40"
                            VerticalAlignment="Stretch"
                            ToolTip="关闭">
                        <TextBlock Text="&#xE8BB;"
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="10"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"/>
                        <Button.Triggers>
                            <EventTrigger RoutedEvent="Button.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <SolidColorBrush Color="#C42B1C"/>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="Button.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <SolidColorBrush Color="Transparent"/>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                    </Button>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180" MinWidth="180" MaxWidth="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Background="{StaticResource BgDefaultBrush}"
                        BorderBrush="{StaticResource BorderMutedBrush}"
                        BorderThickness="0,0,1,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Padding="16,14,16,8">
                            <TextBlock Text="MENU"
                                       Style="{StaticResource TextMuted}"
                                       FontWeight="SemiBold"/>
                        </Border>

                        <ItemsControl Grid.Row="1"
                                      ItemsSource="{Binding NavigationItems}"
                                      Margin="8,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border x:Name="itemBorder"
                                            CornerRadius="6"
                                            Padding="12,10"
                                            Margin="0,1"
                                            Cursor="Hand"
                                            BorderBrush="Transparent"
                                            BorderThickness="1">
                                        <Border.InputBindings>
                                            <MouseBinding MouseAction="LeftClick"
                                                          Command="{Binding DataContext.ActivateNavigationCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                          CommandParameter="{Binding}"/>
                                        </Border.InputBindings>
                                        <Grid>
                                            <TextBlock Text="{Binding Title}"
                                                       FontSize="13"
                                                       VerticalAlignment="Center"
                                                       Foreground="{StaticResource FgDefaultBrush}"
                                                       TextWrapping="NoWrap"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </Grid>
                                    </Border>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                            <Setter TargetName="itemBorder" Property="Background"
                                                    Value="{StaticResource SelectionBgBrush}"/>
                                            <Setter TargetName="itemBorder" Property="BorderBrush"
                                                    Value="{StaticResource AccentBlueBrush}"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Border Grid.Row="2"
                                BorderBrush="{StaticResource BorderMutedBrush}"
                                BorderThickness="0,1,0,0"
                                Padding="16,10">
                            <TextBlock Text="Tranbok Tools v1.0"
                                       Style="{StaticResource TextMuted}"
                                       FontSize="11"/>
                        </Border>
                    </Grid>
                </Border>

                <Grid Grid.Column="1" Background="{StaticResource BgCanvasBrush}">
                    <StackPanel VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                Opacity="0.4">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasActiveView, Mode=OneWay}" Value="False">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="&#xE8F1;"
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="48"
                                   Foreground="{StaticResource FgSubtleBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,16"/>
                        <TextBlock Text="请选择左侧菜单"
                                   Style="{StaticResource TextLabel}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>

                    <ContentPresenter Content="{Binding ActiveView}"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>